# Adult Income ML Project

This project uses the UCI Adult dataset to predict whether a person makes over $50K per year.
Models used:
- Logistic Regression
- Random Forest

To run:
1. Install requirements: pip install -r requirements.txt
2. Open project.ipynb and run all cells.

Dataset source:
UCI Machine Learning Repository (Adult Data Set)
